---
description: "회고 워크스페이스 초기 설정 - 프로젝트 연결 및 첫 변환"
---

## 회고 워크스페이스 온보딩

$ARGUMENTS

### Step 1: 환영 & 설명

다음 메시지를 사용자에게 보여줍니다:

---

**회고 워크스페이스에 오신 것을 환영합니다!**

이 워크스페이스는 Claude Code와 나눈 대화를 돌아보고, **AI와 더 잘 협업하는 법**을 배우는 공간입니다.

매주 한 번 여기서 이번 주 대화를 리뷰하면:
- 내가 어떤 실수를 반복하고 있는지
- AI에게 어떻게 요청하면 더 효과적인지
- 어떤 개발 개념을 모르고 넘어갔는지

를 발견할 수 있습니다.

지금부터 초기 설정을 진행하겠습니다.

---

### Step 2: Claude Code 세션 확인

`~/.claude/projects/` 디렉토리를 확인하여 사용 가능한 프로젝트 목록을 가져옵니다.

```bash
ls ~/.claude/projects/
```

프로젝트가 없으면:
> "아직 Claude Code 대화 기록이 없습니다. 먼저 다른 프로젝트에서 Claude Code를 사용한 후 다시 와주세요."
> → 여기서 종료

프로젝트가 있으면 다음 단계로 진행합니다.

### Step 3: 프로젝트 이름 매핑

발견된 프로젝트 디렉토리 목록을 보여주고, 사용자에게 읽기 좋은 이름을 지정하도록 안내합니다.

**안내 메시지:**

> Claude Code가 저장한 프로젝트들을 발견했습니다.
> 각 프로젝트에 알아보기 쉬운 이름을 붙여주세요.
>
> 디렉토리 이름이 복잡해 보여도 걱정하지 마세요 - 실제 프로젝트 폴더 경로입니다.

각 프로젝트 디렉토리에 대해:
1. 디렉토리명에서 프로젝트명을 추측합니다 (경로의 마지막 의미 있는 부분 추출)
2. 사용자에게 AskUserQuestion으로 확인받습니다:
   - "이 프로젝트(`-Users-xxx-my-project`)의 이름을 뭐라고 할까요?"
   - 옵션: 자동 추측 이름 + "다른 이름" + "건너뛰기 (분석 안 함)"

**규칙:**
- 한 번에 5개까지만 질문합니다 (너무 많으면 피로)
- 세션이 5개 미만인 프로젝트는 자동으로 건너뜁니다 (사용자에게 알림)
- "건너뛰기"를 선택한 프로젝트는 매핑에서 제외

결과를 `10-scripts/project_names.json`에 저장합니다:

```json
{
  "-Users-xxx-my-project": "my-project",
  "-Users-xxx-another": "another"
}
```

### Step 4: 첫 변환 실행

```bash
python3 10-scripts/convert_sessions.py --force
```

변환 진행 상황을 보여주고, 완료되면 결과를 요약합니다:
- 총 프로젝트 수
- 총 세션 수
- 가장 활발한 프로젝트 TOP 3

### Step 5: 사용법 안내

---

**설정 완료!**

사용할 수 있는 기능:

| 명령 | 설명 |
|------|------|
| `/retro` | 새 대화 변환 (매주 실행 권장) |
| `멘토링해줘` | AI 활용 능력 코칭 |
| `성장 리포트 만들어줘` | 성장 분석 리포트 |
| `[프로젝트명] 분석해줘` | 특정 프로젝트 리뷰 |

**추천 루틴:**
1. 매주 금요일, 이 폴더에서 `claude` 실행
2. `/retro` 로 이번 주 대화 변환
3. `멘토링해줘` 로 이번 주 리뷰

---

### Step 6: 바로 시작할지 물어보기

AskUserQuestion으로 물어봅니다:
- "바로 이번 주 리뷰를 시작해볼까요?"
- 옵션: "멘토링 시작", "성장 리포트 생성", "나중에 할게요"

선택에 따라:
- "멘토링 시작" → mentor 스킬 실행
- "성장 리포트 생성" → growth 스킬 실행
- "나중에 할게요" → 종료
